{
  "main": "dist/server.js",
  "scripts": {
    "clean": "npx pm2 stop all && npx pm2 kill && rm -rf ./node_modules/",
    "build": "npm install && tsc",
    "start": "npx pm2 start 'node .'",
    "deploy": "npm run build && npm run start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^8.0.0",
    "pm2": "^5.3.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.20",
    "@types/node": "^20.8.10",
    "typescript": "^5.2.2"
  }
}
